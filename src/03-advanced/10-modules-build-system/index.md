# Chapter 10: Modules and Build System

Learn to organize and build Zig projects effectively.

## Topics

- Hierarchical package organization (`@import`)
- Controlling symbol exports (`pub` keyword)
- Relative imports for submodules
- Splitting modules across multiple files
- Common namespace organization
- Module reloading (alternatives in compiled context)
- Runnable directories and executables
- Reading datafiles within packages (`@embedFile`)
- Build path configuration (`build.zig`)
- String-based imports (compile-time `@import` maps)
- Distributing packages (Zig package manager)

See TODO.md for complete recipe list (11 recipes planned).
