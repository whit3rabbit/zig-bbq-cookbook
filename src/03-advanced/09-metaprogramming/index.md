# Chapter 9: Metaprogramming

Unlock the power of Zig's compile-time execution with `comptime`.

## Topics

- Function wrappers (manual wrapping)
- Preserving function metadata
- Unwrapping decorators
- Decorators with arguments (compile-time function generation)
- Type checking enforcement (`comptime` checks on types)
- Decorators in structs
- Decorators as structs
- Applying decorators to struct methods
- Adding arguments to wrapped functions
- Patching struct definitions
- Controlling instance creation (generic types)
- Capturing attribute definition order (`@typeInfo` reflection)
- Generics with optional arguments (config structs)
- Enforcing argument signatures (`anytype` and `@typeInfo`)
- Enforcing coding conventions (compile-time checks)
- Defining structs programmatically (returning types)
- Initializing struct members at definition time

See TODO.md for complete recipe list (17 recipes planned).
