# Chapter 2: Strings and Text

Master text processing in Zig using UTF-8, `std.mem`, and `std.fmt`.

## Topics

- Splitting strings on delimiters (`std.mem.tokenizeAny`)
- Matching text at start/end (`startsWith`, `endsWith`)
- Shell wildcard patterns and text search
- Search and replace operations
- Case-insensitive operations
- Unicode text handling
- String trimming and sanitization
- Alignment and formatting
- String concatenation and interpolation (`std.fmt.allocPrint`)
- Byte strings vs Unicode strings

See TODO.md for complete recipe list (14 recipes planned).
